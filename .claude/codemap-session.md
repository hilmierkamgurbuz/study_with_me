<!-- stamp: 0b11c11 2026-08-21 status: OK -->
Assets/Scripts/Session/StudyBlockRunner.cs | the study/break clock, told when it may run | sys: Session | api: StudyPhase{Idle,Study,Break}; StudyClockEvent{None,StudyBlockEnded,BreakElapsed}; Phase; StudyMinutes; BreakMinutes; RemainingSeconds; StartStudy(int studyMinutes, int breakMinutes); Stop(); Tick(float deltaSeconds, bool studyModeActive, bool activityRunning, bool voiceBusy) | dep: - | used: RoomSessionController | crit: K2 | note: the first code in the Session system, and deliberately not the six-state machine the plan drew: with the conversation ambient and always free there is no flow STATE to transition, only a clock, and a state machine for one clock would model a distinction that does not exist. Plain C# with no Unity types and no MonoBehaviour, so it needs no scene object and can be exercised without entering Play — the caller supplies the delta and the three facts, which is also what keeps Session from having to look at Presentation to learn whether a dance is running. Tick returns an event rather than raising one: one enum compare at the call site beats an event a MonoBehaviour has to subscribe to and unsubscribe from. Study time is spent ONLY by studying: !studyModeActive, activityRunning or voiceBusy all freeze it, because a dance, a game and a conversation are things done INSTEAD of studying, not shortened studying. The break is the asymmetric half and the asymmetry is the point — it does not advance while an activity runs (a dance lasts as long as a dance and a game as long as the player wants, so the clock has nothing to say), it reports BreakElapsed exactly once, and it does NOT leave the Break phase when it does: what ends a break is her asking and the user answering, so only a fresh StartStudy moves the phase. BreakMinutes <= 0 is legal and means a break with no bell at all, which is what "there is no rule that a break is five minutes" turns into here | h:72a60cee
Assets/Scripts/Session/BreakOfferPolicy.cs | decides whether a break gets an activity offer, and which | sys: Session | api: BreakActivity{None,Dance,Game}; ActivityInterest{Unknown,Likes,Dislikes}; BreakOffer{Activity,AskInterestFirst,HasOffer}; BreakOfferPolicy(int offerEveryNthBreak, int declineLimit); SetInterest(BreakActivity, ActivityInterest); RecordNotNow(BreakActivity); NextOffer() | dep: - | used: RoomSessionController | crit: K2 | note: the prompt can describe an offer but cannot count, so "don't ask every break" and "stop asking after three no's" live here as arithmetic rather than as a hope about the model's behaviour. Plain C#, no Unity types, no persistence: interests are seeded from the profile at construction and the not-now streaks are session-lived on purpose — a firm no is stored as an interest and lasts forever, while three passes in one sitting are about today, and a new day deserves a fresh ask. Offers alternate, so two breaks in a row never propose the same thing, and an Unknown interest yields AskInterestFirst instead of a proposal: asking someone to dance before knowing whether they like dancing is the order the whole feature was asked to avoid. Saying yes clears that activity's streak, or three earlier passes would keep something they have just asked for off the table. Both constructor arguments are floored at 1, since a 0 there would either divide by zero or silently switch offers off forever | h:d4d5a04b
