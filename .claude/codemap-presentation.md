<!-- stamp: 340fb36 2026-08-18 status: OK -->
Assets/Scripts/Presentation/CharacterPresenter.cs | locks Chloe's pose, blends a speaking offset, drives face expression from TurnState | sys: Presentation | api: animator; faceRenderer; talkFrameSeconds; danceFrameSeconds; SetDancing(bool dancing); faceZoom; speakingPositionOffset; speakingRotationOffset; poseBlendSeconds; previewSpeaking; lockPose; SetTalking(bool talking); SetTurnState(TurnState state); SetRestingPose(Vector3 position, Quaternion rotation); RestingPosition; RestingRotation; IsSpeaking; CurrentTurn; SetReading(bool reading) | dep: TurnState | used: RoomSessionController, DanceModeController, GameModeController, BookPageTurner, DeskRoutine | crit: K2 | note: SetRestingPose moves the pose LateUpdate rebuilds her FROM, which is the only sanctioned way to relocate her — a caller writing transform.position directly is reverted on the same frame and would be a second writer on the root transform, the very thing this component exists to prevent; dance mode uses it to put her on the dance floor and bring her back, reading the desk pose out of RestingPosition/RestingRotation first. previewSpeaking/lockPose are tuning aids — the offsets are otherwise invisible without a live voice session (the Animator's IsTalking drives the clip, not _isSpeaking) and the transform rebuild otherwise reverts any Scene-view drag during Play; previewSpeaking drives the pose only, never TickTalkCycle, so the face cannot be left parked on a talk cell; LateUpdate rebuilds the root transform from the Start-captured position+rotation every frame (rebuilt, never nudged, so the speaking offset cannot accumulate) — it runs there because the Animator has already written the clip pose by then; the earlier headBone/targetHeadHeight pin was removed with D-015, it pinned the HEAD so the BODY moved and lifted her off the chair once the Talking clip actually drove bones, and its cause (Mixamo root position curves) is gone since D-013 strips them at import; speakingPositionOffset/RotationOffset shift the whole body in her resting frame while speaking, blended over poseBlendSeconds, because the Sitting Talking clip's own pose leans her forward far enough to hide her face from the room camera; face expression picks a cell from the 4x4 grid texture via SetTextureScale+Offset on _BaseMap/_MainTex, row inverted because Unity UV v=0 is the texture's bottom while the art is authored top-to-bottom; scale/offset are derived from the face submesh's REAL UV bounds read once in Start() (ComputeFaceUvTransform), not assumed to be the full 0-1 square — confirmed via ChloeUsedRegionExporter.LogFaceUvBounds() that the mesh only covers a small leftover sub-rectangle from the old shared atlas; faceZoom (Inspector-tunable, default 1.2) samples a smaller centered slice of each cell instead of the whole cell, cropping in on the art to make it fill more of the head's fixed decal area without regenerating art; Speaking cycles the 4 talk-mouth cells on a timer, Listening/Thinking/Idle each pick one fixed cell; dancing cycles SmileSoft/Laugh/Cheeky — grid cells (3,1),(3,2),(4,4) — at danceFrameSeconds (1.5 s, far slower than the 0.15 s mouth cycle because these are whole expressions, not mouth shapes). SetDancing(bool) is the ONLY thing DanceModeController says about the face: which cell shows stays private in here, so the face keeps a single writer. SetReading(bool) is SetDancing's twin and carries the same contract - the desk routine says only whether she is reading, the cell stays in here - and sits LOWEST of the three face drivers: dancing beats speaking beats reading, so it declines to paint while either of those is. Grid cell (2,3) was already the looking-down face, so it needed no new art. The turn-state default branch repaints LookDown rather than Neutral while she reads, because the turn state churns back to Idle between sentences and Neutral there would blink the face off for no visible reason. While dancing the dance cycle wins in LateUpdate and SetTurnState returns early WITHOUT repainting (it still updates the animator flags and _isSpeaking), because a live voice session would otherwise fight the dance for the face. **D-058 REVERSES that**: talking has to be visible in every state, so the order is now speaking > dancing > reading. LateUpdate runs the talk cycle when she is speaking and the dance cycle otherwise — still exactly one cycle per frame, so the single-writer property that D-025 was protecting survives the reversal; what changed is which one yields. SetTurnState no longer returns early mid-dance, and its Idle branch hands the face back down the same ladder (dance cell, else LookDown, else Neutral) rather than to Neutral — without that the churn back to Idle between sentences would blink the dance face off for up to danceFrameSeconds, since the dance cycle only repaints on its own 1.5 s timer. SetDancing likewise declines to paint while speaking. CurrentTurn is exposed read-only alongside IsSpeaking because ambient behaviour needs the WHOLE conversation, not her half of it: listening and thinking are as much a conversation as speaking, and reading IsSpeaking alone is what had the desk routine turn back to the book while the user was still talking | h:7af38870
Assets/Scripts/Presentation/PetRoamer.cs | walks a pet between authored stops | sys: Presentation | api: PetStop{position,stayMinutes,setFacing,facingYaw}; animator; route; runSpeed; turnDegreesPerSecond; startRunningWithinDegrees; arriveDistance; idleStateName; runStateName; animatorCrossFade; partyAreaCenter; partyAreaSize; partyClearance; partyStayMin; partyStayMax; EnterParty(Vector3 center, Transform watch); ExitParty(); Hold(); Release(); drawRoute | dep: - | used: DanceModeController,GameModeController | crit: K3 | note: Hold()/Release() are game mode's whole ask of this component - freeze exactly here, play idle - and they are a FLAG OVER Update, not a state like _party: reusing EnterWaiting() would zero _waitClock and commit to 'I have arrived', restarting a stop's dwell time and, mid-leg, throwing away the fact that the pet was walking somewhere. Holding the whole Update leaves _moving, _index and _waitClock untouched, so Release() carries on the same leg; it replays run or idle to match the state it was frozen in, without which a pet held mid-leg resumes sliding with no leg motion. MUST sit on a PARENT of the animated model, never on the model itself - the vendor clips overwrite ALL THREE channels of the animated object's OWN transform every frame - position to (0,0,0), rotation to identity, and scale to (1,1,1) (measured: 3 position + 4 rotation + 3 scale curves on the empty path) - so a mover on that transform is a second writer and loses every frame; applyRootMotion does not gate them because the PolyOne models have no Root node set (animationType=Generic, rootNode='') and no Avatar, so Unity never classifies the curves as root motion at all. Scene shape is therefore agent-parent 'Cat'/'Dog' carrying this component, the world placement AND the scale (0.2825334 cat / 0.3989600 dog), with the untouched vendor model as its child left at local zero / identity / scale 1 - exactly the values the clip insists on writing. The scale channel is the easy one to miss: position and rotation break loudly (the pet teleports to the world origin) while scale silently inflates it to ~3.5x on Play only. idleStateName/runStateName are Inspector fields, not constants: the two vendor controllers carry no parameters and no transitions - just two loose states - and the names differ between the animals ('A_CartoonAnimal-Cat__Run' really has two underscores while the dog's has one); a mistyped name makes CrossFade silently no-op, so Start() checks both with Animator.HasState and logs an error instead of failing quietly. CrossFadeInFixedTime, not CrossFade: idle is 1.33 s and run 0.33 s, so a normalized duration would blend differently in each direction. route holds world positions specific to Room.unity, which is why it is serialized scene data rather than a ScriptableObject - the same numbers mean nothing in another scene. Travel is a straight line with no obstacle avoidance (no NavMesh); it turns on the spot first and only starts running once within startRunningWithinDegrees, which is what stops the sideways-slide look. runSpeed default 1.2 is a guess to be tuned by eye - stride length cannot be derived because the run clip's root motion is zeroed. Depends on D-021 having flipped `loopTime` on the four vendor clips: they ship non-looping, so Idle froze after 1.33 s and Run after 0.33 s; if a pet ever stands frozen or slides without leg motion again, check that flag before suspecting this component. Per-stop facing is opt-in (`setFacing` + `facingYaw`): off means keep the heading it arrived on, which is the natural default and preserves the pre-facing behaviour; the turn runs alongside the wait clock rather than gating it, so stayMinutes keeps meaning 'how long it stands here'. Stop 0's facing is snapped in Start() rather than turned into. OnDrawGizmosSelected draws a yellow stub for authored facings so a yaw can be typed and checked in the Scene view without entering Play. Party mode (EnterParty/ExitParty, driven by DanceModeController) swaps the target source only: the pet moves to random points inside the partyAreaCenter/Size RECTANGLE, pauses partyStayMin..Max SECONDS (not minutes) and turns to face the watch target instead of an authored yaw. A rectangle, not a ring, IS the obstacle avoidance: it is convex, so a straight line between two interior points also stays interior - keep an obstacle outside the rectangle and the paths avoid it too, no NavMesh needed. THE WATCHED CHARACTER COUNTS AS AN OBSTACLE, which is why the rectangle sits beside her rather than around her: an area containing her would need to be an annulus, and an annulus is not convex, so paths cut straight through the hole. Verified for Room.unity: X 14.20..16.85, Z 2.20..3.20 abuts the dance floor's south edge, clears the armchair (min Z 3.22), the speaker, the door and every wall with zero renderer overlap between y 0.20 and 1.20, and leaves Chloe 0.77 m outside - 5000 simulated paths with the clearance test disabled never came closer than that. Target Y comes from partyAreaCenter.y and NOT from the pet's current height - taking it from the pet is what made a pet that started the party standing on the bed circle the floor at bed height. Point choice is rejection sampling that measures the distance from the watched character to the whole PATH SEGMENT, not just the destination - testing only the destination let a corner-to-corner run pass 0.06 m from her with both ends legal. Bounded at 16 tries, keeps the BEST draw rather than the last. With the rectangle placed clear of her the test now always passes first try; it stays as a guard for anyone who moves the rectangle back over her. `route` is never modified, so ExitParty just resumes the stop it was heading for. DanceModeController supplies only the centre and the watch target - point generation, turning and animator state all stay in here, which is what keeps the agent transform single-writer per D-020 | h:1ce2fbc2
Assets/Scripts/Presentation/DanceModeController.cs | runs the timed dance-mode sequence | sys: Presentation | api: chloe; chloeAnimator; mainCamera; danceCamera; roomLights; discoFixtures; lampChildName; discoBall; noteParticles; musicSource; tracks; dancePosition; danceYaw; lightsOutAt; discoOnAfterLightsOut; danceStartsAfterMusic; danceSeconds; roomLightsBackAfterEnd; danceClipCount; armsLayerName; palette; colourChangeSeconds; discoIntensity; emissionBoost; fixtureSwingDegrees; fixtureSwingSeconds; discoBallDegreesPerSecond; IsRunning; StartDance(); StopDance() | dep: CharacterPresenter | used: - | crit: K3 | note: one clock + a Phase enum drives the whole sequence - Arriving (camera swap + teleport + standing idle) -> room lights out at lightsOutAt -> disco rig, music and particles one second later -> she starts dancing one second after that -> danceSeconds -> BeginUnwind stops music AND the disco on the SAME frame -> room lights, camera and the teleport home all land together one second later so the jump back to the desk is never on screen. NEVER writes chloe.transform: CharacterPresenter.LateUpdate rebuilds her root every frame from its own captured pose, so a direct write would be reverted on the same frame and would create the second writer that component exists to prevent - placement goes through SetRestingPose, and the desk pose is read back from RestingPosition/RestingRotation in Start rather than hardcoded. Lights are switched by Light.enabled, never GameObject.SetActive, because the lamp meshes must stay visible while dark. Only the fixtures are wired in the Inspector; each fixture's Lights and its 'r' lamp shell are read out of its own children at Start, because two hand-ordered flat arrays of different lengths (8 lights, 4 lamps) cannot guarantee a lamp glows its OWN light's colour. Lamp glow is driven through a MaterialPropertyBlock, never sharedMaterial: the four 'disco lamp N' materials are assets and a runtime write to sharedMaterial edits them on disk permanently. Side effect: the materials still look lit in the Editor, since their asset emission stays on and only Play overrides it. The masked Arms layer is dropped to weight 0 for the duration of the dance (by NAME via GetLayerIndex, not index, and it logs an error if the name is missing) so the dance plays from ONE playback head on the base layer - the Arms layer's duplicate dance states from stage 1 were deleted once this landed. Returning it to weight 1 hands the arms straight back to the talking behaviour without that behaviour needing to know a dance happened. Drives the pets' party mode from the same one place the animator flags are set, which is the single funnel both the timed ending and StopDance() pass through - so no path can leave a pet stuck circling. Tells CharacterPresenter.SetDancing(bool) alongside the animator flags, but never picks a face cell itself — the face stays single-writer inside CharacterPresenter. Camera switching also toggles each camera's AudioListener, because Camera.enabled leaves the listener live - two enabled listeners make Unity warn every frame and leave the music's mix undefined. Start() defines the resting state itself (disco off, room lights on, dance camera off) rather than trusting the scene, which ships with both cameras enabled and the disco rig lit. Beams sweep a CIRCLE: each fixture is spun about WORLD Y (hence world rest rotations are cached, not local ones), which traces a ring centred under it because the beam already leans ~27 deg off vertical; beamSpreadDegrees leans it further out and sets the radius, and the fixtures run a quarter turn apart. The tilt axis is `Cross(flat, up)` and the order matters — `Cross(up, flat)` was measured to lean the beam back TOWARDS vertical and shrink the ring. Measured radii: spin alone 0.90 m, +5 deg 1.11 m, +15 deg 1.59 m, against a 1.55 m dance floor, which is why the default is 5 and not more. Each fixture also lost its second, horizontal spot (D-026) - those aimed at the disco ball, not the floor. danceClipCount is a field so a fourth dance is an Inspector change. The desk pose is captured in StartDance(), NOT in Start() (D-058): CharacterPresenter fills RestingPosition in its own Start() and the order between two Start() calls is undefined — D-037 fixed exactly this in GameModeController and knowingly left it here because the desktop happened to win the race, writing down that a character teleporting home to nowhere after a dance would be the tell. On a phone the race was lost and she came back to the world origin instead of her chair. Reading it when a dance is asked for removes the race rather than making it less likely, since by then the scene has been awake for many frames | h:90656792
Assets/Scripts/Presentation/BookPageTurner.cs | turns a page of the desk book as room ambience | sys: Presentation | api: leftPage; rightPage; flipPage; hinge; leftPageSlot; rightPageSlot; flipPageSlot; pageA; pageB; chloe; danceMode; gameMode; selfDriven; intervalSeconds; turnSeconds; drawHinge; IsTurning; CanTurn; TurnPage() | dep: CharacterPresenter,DanceModeController,GameModeController | used: DeskRoutine | crit: K3 | note: the sheet is HIDDEN except during a turn, which is what makes the loop seamless: it appears carrying exactly what the right page shows, the page beneath quietly becomes the next one WHILE COVERED, and at the end the left page becomes what the sheet carried - so nothing ever snaps back on screen. Two images alternate; a third is not needed for 'the page changed' to read. TurnPage() is public and selfDriven exists because the desk behaviour that will have her glance at the monitor and then at the book should decide WHEN a page turns - a page turning while she looks at the screen is one turning behind her back - so the timer here is a stand-in and the integration is one call. The clock does NOT advance while a turn is blocked: run during a five-minute conversation it would flip a page the instant she stopped talking, which reads as a reply rather than as ambience. Blocked on Speaking only - listening and thinking are her being quiet, and a page turning then is exactly the life this is for - and on either mode running, because 'at her desk' has to be built from the negative while the Session state machine is still unmapped. Sweep direction is MEASURED per turn, not serialized: +180 and -180 land in the same place so the endpoint cannot decide it, the difference is whether the path arcs up over the book or sweeps down through the table, so the two midpoints are compared and the one that rises wins - and measuring keeps it right if the book is moved or turned round. Rotation goes about the measured hinge with RotateAround, never the sheet's own pivot, which may not sit on the spine. sharedMaterials throughout: renderer.materials would clone and leak an instance on every single turn | h:e815faca
Assets/Scripts/Presentation/ArmIkSolver.cs | solves both arms onto hand targets, wrist included | sys: Presentation | api: Arm{upperArm,forearm,hand,elbowHintLocal,IsComplete,Reach}; characterRoot; leftArm; rightArm; leftTarget; rightTarget; weight; drawGizmos; LogHandTargets() | dep: - | used: DeskRoutine | crit: K3 | note: exists because choosing clip frames could never have worked - the typing clip is 0.50 m short of the keyboard across all 17.6 s of itself (measured), and no frame choice can rotate a WRIST, which was the actual complaint. Analytic two-bone IK rather than a package: the rig is Generic (animationType 2) so Unity's humanoid IK is unavailable, and the chain the arms mask already defines is exactly two joints and a tip. Writer chain on those six bones: the Animator writes them, then this overrides in LateUpdate - the same shape and the same reason as CharacterPresenter overriding the ROOT, and it is the ONLY override of them; it never touches the root, so the two never overlap. DefaultExecutionOrder 100 puts it after CharacterPresenter, because the root has to have moved before the arms are solved to world-space targets. Blending is done in LOCAL space: the three bones are a parent chain, so saving world rotations and slerping them back fights itself as each parent moves. Reach is CLAMPED, not failed - a target beyond the arm leaves it straight and pointing, which honestly looks like reaching for something out of range, and the setup tool reports the distance in metres so it gets fixed in the scene instead of hidden here. The elbow axis comes from elbowHintLocal (in the character's frame, so it still means 'down and back' after she turns) rather than from whatever the maths prefers, and the gizmo draws it, because a joint bending backwards is otherwise a guessing game. weight 0 skips everything, so nothing is paid when nobody is reaching. LogHandTargets prints target world transforms to the console: Play-mode scene edits are discarded on exit but the console is not, and there is no Copy/Paste Component Values for a dragged Transform | h:728635b8
Assets/Scripts/Presentation/DeskRoutine.cs | what she does at her desk between conversations | sys: Presentation | api: chloe; book; armIk; bookTarget; pageHand; previewPageReach; IsRunning; StartStudying(); StopStudying(); conversationTarget; conversationHoldSeconds; danceMode; gameMode; pageTurnSeconds; pageGestureSeconds; turnDegreesPerSecond; arrivedDegrees; readingPositionOffset; readingYawOffset; offsetMetresPerSecond; tunePose; TurnAPageNow() | dep: ArmIkSolver,BookPageTurner,CharacterPresenter,DanceModeController,GameModeController | used: StudyModeButton | crit: K3 | note: OFF until a button starts it. She is meant to be talked to first, and a character who turns to her book the moment the scene loads has decided the conversation is over before it started; the default state is therefore no mode at all - at her desk, facing the monitor, nothing here writing her pose. Stopping mid-page walks her back to the authored pose and only THEN stops writing, so she is never left facing the book with nobody driving her. Dance and game mode SUSPEND studying rather than cancel it - it resumes where it left off. The routine is ONE activity - she reads - and that is the whole of it. The computer beat is gone rather than switched off, and the measurement that ended it is worth keeping: her arm reaches 0.259 m while the keyboard sits 0.70-0.81 m from her shoulder, so her hands could never have arrived there, by any clip or by IK (D-049). Everything that served it went too - both animator states, the ActivitySpeed parameter, the held-frame scan, the chair machinery, the typing offsets. This component no longer touches the Animator AT ALL: ArmIkSolver overrides the whole arm chain, so the masked Arms layer only ever contributed the shoulder, which its own default state gives for free. IK is OFF between page turns - resting arms come from the animation, which already looks right, so there is no resting target to place and no override to fight. A page turn fades the RIGHT hand out to the page and back, and the WEIGHT is the arc: the hand leaves from wherever the animation has it and returns there by itself, which is what removed the lap targets - their only job had ever been to be the place it came back to. The left arm is never solved; one hand turns a page, and leaving the other to the animation is one less thing to place. previewPageReach pins the hand at the target so dragging it moves the hand and turning it turns the wrist - without that the reach is a 1.5 second event, which is no way to place anything. There is no page-turn CLIP for this in the project or in the Mixamo set it came from; a target arc is what makes the gesture possible with no new art, and it puts the LOOK of it in the scene where it can be adjusted by eye, wrist included, since IK takes the target's rotation. She turns her BODY to the book, never her head - Generic rig, no humanoid look-at, and aiming the head by hand would be the second writer on it that D-015 had to undo. The desk pose is captured lazily on the first frame that runs (D-037's race), talking freezes the page clock and returns her to the AUTHORED pose rather than a computed monitor look, and tunePose stops her pose being written so a Scene-view drag survives during Play - CharacterPresenter.lockPose has to be off too, and the numbers come back out through ArmIkSolver.LogHandTargets, because Unity discards Play-mode scene edits but keeps the console. Being talked to is now a HOLD, not a reaction (D-058): it reads chloe.CurrentTurn rather than IsSpeaking, so listening and thinking count too, and conversationHoldSeconds keeps her turned to the user through the gaps — reading IsSpeaking alone had her facing the book while the user was still speaking, and back at it the instant she finished a sentence. During the hold she turns to conversationTarget (the room camera, i.e. the user) instead of to the authored monitor pose, because the monitor is where she was, not who she is answering; a missing target falls back to that authored pose, so the reference costs the look and never the routine. FaceTowards takes a DeskFacing now — Desk, Book or Conversation — and only Book carries readingPositionOffset. The page clock is frozen for the whole hold, so conversation time still is not counted as reading time | h:e7c9d8bc
Assets/Scripts/Presentation/GameModeController.cs | runs the game-mode sequence and hands the screen to Fruit Merge | sys: Presentation | api: chloe; chloeAnimator; mainCamera; gameCamera; pets; danceMode; roomUi; couchPosition; couchYaw; zoomCamera; zoomArcHeight; playResolution; letterboxColor; zoomStartsAfter; zoomSeconds; gameScenePath; roomLayerName; drawFraming; IsRunning; CanLeave; StartGameMode(); StopGameMode() | dep: CharacterPresenter,PetRoamer,DanceModeController | used: GameModeButton | crit: K3 | note: one clock + a Phase enum, the same shape as DanceModeController - Watching (camera_game on, she takes the couch, pets Hold(), Game.unity starts loading with allowSceneActivation FALSE) -> zoomStartsAfter -> Zooming (position+rotation+FOV lerp onto the TV over zoomSeconds) -> Playing (scene activated, both room cameras off, room UI hidden) -> Returning (unload, push-in reversed, she goes back to the desk, pets Release()). NEVER writes chloe.transform: placement goes through SetRestingPose, and the desk pose is read back out of RestingPosition/RestingRotation in Start rather than hardcoded. camera_game's transform+FOV is this component's alone; the authored pose is captured in Start and is what t=0 restores, so framing stays an Inspector job. ALL timing is unscaled: the game zeroes Time.timeScale while paused and leaving from its pause screen is a normal exit, on which scaled time would freeze the camera forever - the same reason timeScale is forced back to 1 during the unwind BEFORE any lerp runs. MoveRoomOntoItsOwnLayer() moves the whole room to layer 8 once in Start and the loaded game's cameras get that layer culled: the Fruit Merge camera is orthographic at world (0,0.5,-10) and at 16:9 spans x -9.8..9.8 while the room starts near x 11 - no overlap - but at 21:9 its half-width reaches 12.8 and clips the room's TV corner, which then wins the depth test against the game's background sprite. The ROOM moves rather than the game because the game spawns objects on the default layer at runtime (worm cursors, combo popups) that culling would delete; it is never restored because the room's own cameras cull nothing and no code here raycasts a layer mask; and it is done at runtime rather than baked into Room.unity to keep a 171-object diff out of a hand-built scene and to cover anything added later. Leaving is QUEUED while the handover coroutine is in flight (_handingOver/_leaveRequested) - Esc works during it, and a teardown racing a half-built handover is where the hard bugs live. SilenceTheGame() stops every AudioSource NOT in the room scene, selecting by scene rather than by type: the game's AudioService is DontDestroyOnLoad so unloading its scene does not silence it, and going by scene keeps a compile-time reference out of the vendored tree while provably missing the room's own sources. The desk pose is read when the mode STARTS, not in Start(): CharacterPresenter fills RestingPosition in its own Start() and the order between two Start() calls is undefined - losing that race captured (0,0,0) and sent her to the world origin on the way back (D-037). The game is held to its authored 1080x1920 portrait shape (D-036): the game camera gets a centred viewport rect, its ROOT canvases are re-bound from ScreenSpaceOverlay to ScreenSpaceCamera at planeDistance 5 - an overlay canvas is blind to any viewport, so constraining the camera alone would leave the HUD reaching for the corners of a landscape window while the board sat in a strip. PrepareLoadedGame does this in TWO passes and the order is load-bearing: canvases and the camera live under different scene roots, so binding inside the collection loop set worldCamera to null - and a ScreenSpaceCamera canvas with a null camera behaves EXACTLY like an Overlay one, which silently reproduced the very bug it was meant to fix. The camera to bind to is picked by MainCamera tag first (what the game's own Camera.main resolves to) with lowest depth as the tie-break - and every canvas that participates in sorting (root, plus nested ones with overrideSorting - 8 of this game's 10) gets canvasSortingBoost added to its order, because binding to a camera drops canvases into the SAME sort as the sprites, where their authored 0..3 lost to fruit at 100-tier (90..101) and particles at 201 and the pause/result panels came up BEHIND the board; as Overlay canvases that never mattered, since Overlay always draws last. Added, not assigned, so the panels keep their order relative to each other - and a cullingMask=0 camera at depth -100 paints the bars, because outside a viewport rect Unity clears NOTHING and they would otherwise be undefined framebuffer. The viewport is recomputed only when Screen.width/height actually change, two int compares a frame. The push-in's END POSE is a second, permanently disabled camera (zoomCamera) rather than a position + look-at pair: six coordinates with no feedback until Play is trial and error, whereas a camera is framed by dragging it while reading Unity's own camera preview. Its position, rotation, FOV and NEAR CLIP are all read live every frame, so a nudge in the Scene view shows up on the next Play with no re-cache - and the near clip matters as much as the pose, since the scene default 0.3 slices the screen away before the camera reaches it. Rotation is Slerped between the two authored cameras rather than aimed at a look-at point, so whatever tilt the shot was framed with is what it lands on. zoomArcHeight lifts the path over Chloe's head - she sits directly between camera_game and the TV, so a straight line flies through her. The handover also turns the DEVICE portrait and the return puts it back (D-058): the game is authored 1080x1920 and the app ships landscape, so on a phone it would otherwise play in a narrow centre strip with most of the screen spent on letterbox bars. EnterPlayOrientation is guarded on playResolution actually being portrait, so it stays correct if that field changes, and it captures the previous orientation rather than assuming landscape, so restoring works whether the build is fixed or auto-rotating. Desktop is untouched — Screen.orientation is not honoured there, which is why the Editor behaves exactly as before. ExitPlayOrientation sits in ReturnToRoom, the single funnel every exit path passes through, including the one taken before a handover ever happened, where it is a no-op | h:604f637f
