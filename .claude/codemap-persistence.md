<!-- stamp: 0b11c11 2026-08-21 status: OK -->
Assets/Scripts/Persistence/IProfileRepository.cs | profile load/save contract | sys: Persistence | api: Load(); Save(StudentProfile) | dep: StudentProfile | used: LocalJsonProfileRepository,RoomSessionController | crit: K1 | note: justified by scope.md's deferred backend-sync roadmap item; semantic fields re-verified against the file on 2026-08-17 (interface unchanged, two members) and the STALE marker cleared — the drift was whitespace, not content | h:e74da295
Assets/Scripts/Persistence/LocalJsonProfileRepository.cs | JSON file in Application.persistentDataPath | sys: Persistence | api: Load(); Save(StudentProfile profile) | dep: IProfileRepository,StudentProfile | used: RoomSessionController | crit: K1 | note: compact (non-indented) JSON | h:da81e7db
Assets/Scripts/Persistence/ProfileMerger.cs | sole writer of StudentProfile fields | sys: Persistence | api: Migrate(StudentProfile); ApplyToolCallArgs(StudentProfile, JObject); SetDanceInterest(StudentProfile, string); SetGameInterest(StudentProfile, string); AddEvent(StudentProfile, string text, DateTime nowUtc); SetSessionSummary(StudentProfile, string summary, DateTime nowUtc); StampSession(StudentProfile, DateTime nowUtc); PruneEvents(StudentProfile, int keepDays, DateTime nowUtc) | dep: StudentProfile | used: RoomSessionController | crit: K1 | note: CLAUDE.md invariant — no other code assigns profile fields directly, which is why pruning, migration and the interest flags all had to land here rather than in the caller that noticed they were needed. ApplySessionStats is gone with the statistics it wrote. Migrate is called by the caller after Load rather than by the repository, because the repository is not allowed to touch individual fields; it needs no v1 branch since deserialising a v1 file into the v2 class drops the retired fields by itself, so all it does is stamp the version they were dropped at and fill nulls. AddEvent drops a repeat of the same text — the same fact told twice in one conversation should not take two slots in a memory this small. PruneEvents KEEPS an entry whose timestamp will not parse rather than discarding it: a bad date is a reason to look, not a reason to lose what the user said. Dates go in and out as round-trip ISO under InvariantCulture, so a device locale cannot change what a saved file means | h:7fad9437
Assets/Scripts/Persistence/StudentProfile.cs | what is remembered between sessions, and little else | sys: Persistence | api: CurrentSchemaVersion; InterestUnknown/InterestLikes/InterestDislikes; schemaVersion; studentName; gradeOrClass; likesDancing; likesGames; lastSessionUtc; lastSessionSummary; recentEvents | dep: - | used: IProfileRepository,LocalJsonProfileRepository,ProfileMerger,RoomSessionController | crit: K1 | note: schemaVersion 2. What LEFT is the point of the version: totalStudySessions and totalStudyMinutes are gone because the app is not to keep study statistics at all, and examTarget/usualStudyTime/typicalSessionMinutes/preferredBreakFrequencyMinutes went with them because the character asks how long to work every session rather than reading it off a record — an exam now arrives as a recentEvents line like any other notable thing. Memory is therefore the last session's one-sentence summary plus events from the last few days, pruned on every write. The interest flags are plain strings with consts beside them rather than an enum, because the enum that means the same thing (ActivityInterest) lives in Session and Persistence must not depend on Session; the mapping happens in Bootstrap, which is allowed to know both | h:911967bb
