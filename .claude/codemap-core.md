<!-- stamp: 340fb36 2026-08-18 status: OK -->
Assets/Scripts/Config/GeminiApiConfig.cs | holds the Gemini API key + Live model id | sys: Config | api: apiKey; liveModelId | dep: - | used: - | crit: K1 | note: instance asset (Assets/Config/GeminiApiConfig.asset) is gitignored; no consumer wired yet | h:ccbf2622
Assets/Scripts/Config/ChloePersonaConfig.cs | every word Chloe is told and every session number, as data | sys: Config | api: persona; safetyRules; conversationRules; toolRules; firstMeeting; returning; openingNudge; studyBlockEndedNudge; offerActivityNudge; askActivityInterestNudge; breakOverNudge; activityOverNudge; danceLabel; gameLabel; pomodoroStudyMinutes; pomodoroBreakMinutes; memoryDays; declineLimit; offerEveryNthBreak; reconnectBackoffMaxSeconds | dep: - | used: RoomSessionController,SessionAiSetup | crit: K1 | note: exists because the system instruction used to be built out of Turkish string literals inside RoomSessionController, which is the content-in-code the project forbids. Sibling of GeminiApiConfig in every way (ScriptableObject in Scripts/Config, asset in Assets/Config) except that its asset is TRACKED — .gitignore names GeminiApiConfig.asset specifically, not the folder — because persona wording is content the repo should carry and an API key is not. The .cs declares no field initialisers at all: seed values live in exactly one place, SessionAiSetup, and the asset is the authority from the moment it exists. Text fields carry {profile}/{activity}/{pomodoroStudy}/{pomodoroBreak} placeholders that RoomSessionController substitutes at connect time, which is how a number stays a number in the Inspector instead of being retyped inside a sentence | h:3a15028b
Assets/Scripts/Bootstrap/RoomSessionController.cs | Room.unity composition root: persona, tools, clock, modes, the button | sys: Bootstrap | api: session; character; talkButton; persona; studyMode; danceMode; gameMode; Connect() | dep: BreakOfferPolicy,ChloePersonaConfig,CharacterPresenter,DanceModeController,DeskRoutine,GameModeController,GeminiLiveVoiceSession,IProfileRepository,LocalJsonProfileRepository,ProfileMerger,PushToTalkButtonView,PttButtonState,StudentProfile,StudyBlockRunner,TurnState | used: - | crit: K1 | note: the conversation drives the app, so this is where a tool call becomes a study block, a dance, a game or a line of memory. Eight tools, all declared at setup because the Live API cannot change a tool list mid-session; WHEN to reach for each is described in the persona asset, never here — the only strings in this file are protocol (tool/argument names, JSON schemas). The saved profile reaches the prompt as raw JSON via the {profile} placeholder, deliberately: rendering it as prose would need Turkish field labels, and those would be exactly the content-in-code the persona asset exists to remove. "We have not met" is defined as an empty studentName, which is also the condition for the first conversation never mentioning studying. The clock is ticked from HERE rather than owned by a MonoBehaviour in Session, because deciding whether it may advance means reading DeskRoutine/DanceModeController/GameModeController — Session must not see Presentation, and Bootstrap already may. What it ticks with: studying counts only while she is at the book, no mode owns her, and the turn is Idle, so a dance, a game and a conversation are all time spent INSTEAD of studying. A break is not ended by the clock: an activity ending is caught as the falling edge of IsRunning and turns into "chat, then ask whether to carry on", and the break timer only ever produces the same ask — start_study_block is the only thing that resumes. Nudges travel as user turns because the Live API has no other inbound channel; they are bracketed and conversationRules tells her never to read one aloud, and they never reach the caption log, so they cannot be mistaken for something the user said. On a resumed connection the opening greeting is skipped (session.ResumedFromHandle) — the same conversation carrying on must not be greeted twice. Which activity is running is named on the RISING edge rather than by whatever started it, so a mode opened from outside the conversation is described as accurately as one it asked for, and the label keeps one writer. The single button is now a real Canvas one (D-056): this subscribes to PushToTalkButtonView.Pressed and decides what a press means — Connect the first time, then open and close the mic. A press while the socket is down is deliberately ignored rather than retried, because reconnecting is the transport's own job and a second Connect() would discard the resumption handle and start a different conversation; ButtonState() reports that window as Connecting so the button is dimmed and uninteractable rather than merely inert. State is pushed every frame and the view early-outs on no change, which is cheaper than keeping the two in sync by hand. OnGUI kept only the transcript, and it still hides while a mode owns the screen. stop_activity takes no argument for the same reason it exists: only one mode can run (game mode refuses to start during a dance), so "stop what we are doing" cannot be ambiguous, and an argument would only be something for the model to get wrong. The transcript is hidden while a mode runs so the button is all that covers the game. start_study_block and end_session both call StopActivity FIRST (D-055), and that is a correctness fix rather than a courtesy: DeskRoutine.Update returns early the entire time a mode owns her and StudyBlockRunner will not advance while one runs, so a bare start_study_block during a game is a call that visibly does nothing — the mode stays, she never reaches the desk, the clock never ticks. Leaving the mode is part of starting to study, not a second thing the model has to remember. Two more things landed with D-058. A game asked for DURING a dance is queued rather than dropped: GameModeController refuses to start while a dance runs (deliberate, and it stays), so the request used to be logged and lost — StartActivity now calls StopDance and Update starts the game once the dance has finished unwinding, which is why the lights come back and she reaches the desk before the screen changes. The sequencing lives here and not in GameModeController because the blueprint has game mode reading dance mode ONLY; letting it call StopDance would make it a second writer of dance's flow. StopActivity clears that queue, so "let's stop" said between the two is not followed by the game starting anyway. And the OnGUI transcript is gone entirely — captions go to Debug.Log, so they are still readable over adb logcat while nothing is drawn on screen | h:5315adbc
